name: movecare
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  google_fonts: ^6.2.1

  http: ^1.6.0
  flutter_secure_storage: ^10.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # SECCIÓN DE ASSETS CORREGIDA
  assets:
    - assets/ruta.png
    - assets/movecare.png
    - assets/icono_google.png
    - assets/fondo_welcome.png
    - assets/mapa.png
    - assets/conductor.png